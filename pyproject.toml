[project]
name = "ja-corpus"
version = "0.1.0"
description = "Beam pipeline for processing Japanese Common Crawl dataset"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
select = ["E", "F", "W", "I"]
